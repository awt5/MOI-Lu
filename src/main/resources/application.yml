spring:
  datasource:
    url: jdbc:mysql://moi-database:3306/database?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL5Dialect
    database: mysql
    show-sql: true
